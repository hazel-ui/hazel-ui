import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Color, Theme } from "../../../foundation";
import { Badge as BadgeComponent } from "../Badge";

<Meta
  title="Atoms/Badge"
  component={BadgeComponent}
  argTypes={{
    color: { control: "color" },
    backgroundColor: { control: "color" },
  }}
/>

# Badge

This component can be used to render badge like elements on a page
such as labels, categories, and tags. It renders as a `<span>` tag
by default.

export const Template = (args) => <BadgeComponent {...args} />;

<Canvas>
  <Story
    name="Badge"
    args={{
      size: "m",
      fontWeight: Theme.fontWeight.regular,
      children: "Engineering",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Badge" />

## Examples

[]()

### Small

<Canvas>
  <BadgeComponent backgroundColor={Color.yellow}>Cloud</BadgeComponent>
  <BadgeComponent
    backgroundColor={Color.mintGreen}
    borderRadius="1rem"
    color={Color.yellowGreen}
  >
    Tip
  </BadgeComponent>
  <BadgeComponent
    backgroundColor={Color.paleTurquoise}
    borderRadius="1rem"
    fontWeight={Theme.fontWeight.semiBold}
  >
    enhancement
  </BadgeComponent>
</Canvas>

### Large

<Canvas>
  <BadgeComponent size="l">TITLE</BadgeComponent>
</Canvas>
