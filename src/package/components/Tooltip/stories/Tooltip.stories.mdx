import { ArgsTable, Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Color, Icon, Box, Theme } from "../../../foundation";
import { Tooltip as TooltipComponent } from "../Tooltip";

<Meta
  title="Atoms/Tooltip"
  component={TooltipComponent}
  argTypes={{
    tooltipBackground: { control: "color" },
    tooltipColor: { control: "color" },
  }}
/>

# Tooltip

This component can be used to render a pop-up message that appears on
hovering a JSX element. This is useful for displaying additional details
or helpful messages.

export const Template = (args) => <TooltipComponent {...args} />;

<Canvas>
  <Story
    name="Tooltip"
    args={{
      children: "Hover to see tooltip",
      content: "Tooltip text",
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Tooltip" />

## Direction

The tooltip text can be placed in 4 ways.

<Canvas>
  <Box display="flex" justifyContent="space-between" margin="2rem">
    <TooltipComponent content="Hello from the other side!" direction="top">
      Top
    </TooltipComponent>
    <TooltipComponent content="Hello from the other side!" direction="right">
      Right
    </TooltipComponent>
    <TooltipComponent content="Hello from the other side!" direction="bottom">
      Bottom
    </TooltipComponent>
    <TooltipComponent content="Hello from the other side!" direction="left">
      Left
    </TooltipComponent>
  </Box>
</Canvas>

### Example

<Canvas>
  <Box marginLeft="4rem" marginBottom="6rem">
    <TooltipComponent
      content={
        <>
          <h4>Example title</h4>
          <p>More information</p>
        </>
      }
      place="bottom"
      tooltipBackground={Color.red60}
      tooltipColor={Color.gray5}
      width="10rem"
    >
      <Icon.InfoCircle size="1.3rem" />
    </TooltipComponent>
  </Box>
</Canvas>
